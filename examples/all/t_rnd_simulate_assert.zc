adversary A : () -> Fq.

prove
  [ s <- A();
    r <-$ Fq; ]
  : (r * s = 1) /\ (s <> 0).

rrnd! 2 (r -> r / ((s=0)?1:s)) (r -> r * ((s=0)?1:s)).
rnorm.
rrewrite_ev 1. rnorm.
rindep.
qed.
extract "extraction/t_rnd_simulate_assert.ec".